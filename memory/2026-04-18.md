# 2026-04-18 — Knowledge Base Expansion

## What Happened

### Knowledge Broker System
- Built `scripts/context-broker.sh` — a keyword-based dispatcher that tells any LLM which files to load
- Supports 15+ topic keywords: web, api, network, ad, privesc, recon, cloud, wireless, credential, lateral, c2, shell, enumeration, mitre, report, persist, evasion
- Designed so even small open-source models (Llama, Mistral, etc.) can find the right context fast

### AGENTS.md Wired Up
- Added "Knowledge Base — Auto-Load Guide" section to AGENTS.md
- All sessions now have instructions to read `llm-hacking-context.md` as first step
- Added sub-agent patterns with ready-to-use prompts for:
  - Recon Agent
  - Web Vuln Agent
  - AD Attack Agent
  - Privesc Agent

### 8 New Deep-Dive MITRE Playbooks
Created `knowledge-base/mitre-attack/techniques/`:
1. `web-exploitation.md` — Full web app attack methodology (fingerprinting → content discovery → OWASP Top 10 → auth attacks → file upload → automated scanning)
2. `credential-access-ad.md` — AD attacks zero to Domain Admin (no creds → low priv → local admin → DA → golden ticket)
3. `lateral-movement-deep.md` — All remote exec methods compared (PSExec/WMI/WinRM/SSH/RDP/MSSQL) + tunneling (SSH/chisel/ligolo-ng)
4. `persistence-deep.md` — Linux (cron/systemd/SSH keys/SUID) + Windows (registry/tasks/WMI/services) + AD (golden ticket/DCSync)
5. `defense-evasion-deep.md` — AV/EDR bypass, AMSI bypass, LOLBins (Windows + Linux), log evasion, in-memory execution
6. `reconnaissance-deep.md` — Complete passive OSINT + active scanning methodology
7. `c2-tunneling.md` — Metasploit/Sliver/netcat/socat + DNS/ICMP/SSH/chisel/ligolo tunneling
8. `cloud-attacks.md` — AWS/Azure/GCP attack methodology (SSRF to metadata, enumeration, priv esc)

### Seagate FireCuda SSD Mounted
- Mounted at `/media/sf_Kali_linux` via VirtualBox shared folder
- 1.9TB total, 1.4TB free
- Created `/media/sf_Kali_linux/zuzu/openclaw/` for all Zuzu/OpenClaw data
- Workspace symlinked: `~/.openclaw/workspace → /media/sf_Kali_linux/zuzu/openclaw/workspace`
- Windows Defender exclusion added for `E:\kali_linux` (was eating offensive content files)
- Wordlists backed up to `/media/sf_Kali_linux/zuzu/openclaw/wordlists/`

### 36 Hacking Tools Installed
nmap, masscan, gobuster, feroxbuster, ffuf, dirsearch, nikto, whatweb, wafw00f,
sqlmap, wpscan, subfinder, nuclei, bloodhound, bettercap, wireshark, tshark,
tcpdump, hydra, john, hashcat, medusa, enum4linux, smbclient, crackmapexec,
evil-winrm, dnsrecon, dnsenum, fierce, netdiscover, proxychains4, chisel,
socat, rlwrap, responder, sslscan

- Freed space by cleaning /usr/share/doc, /usr/share/man, unused locales
- sudo password: provided by Raquel

### Mistral Large 675B Test — FAILED
- Raquel tried running `mistral-large-3:675b-cloud` via Ollama as autonomous pentester
- Crashed repeatedly with HTTP 500 and 400 errors from the provider
- Ollama auth was also misconfigured (no API key in auth-profiles.json)
- The model couldn't generate valid tool calls or responses
- Conclusion: Mistral Large 675B via Ollama is not reliable for autonomous hacking tasks

### 2Million HTB Box — FULLY PWNED ✅
- **Target:** 10.129.32.120 (2million.htb)
- **User flag:** `436a33dd7cca8329e69d590f3df1b38d`
- **Root flag:** `aaa44b968c6fdac3b8c5bb6805347ab4`

**Attack chain:**
1. Nmap → ports 22 (SSH), 80 (HTTP/Nginx)
2. Deobfuscated inviteapi.min.js → found invite code generation endpoint
3. ROT13 decode → POST to generate invite → Base64 decode → invite code
4. Registered account, logged in
5. API enumeration: `GET /api/v1` → found admin endpoints
6. IDOR: `PUT /api/v1/admin/settings/update` with `is_admin:1` → escalated to admin
7. Command injection: `POST /api/v1/admin/vpn/generate` with `;id` in username → RCE as www-data
8. Reverse shell → found `.env` with `DB_PASSWORD=SuperDuperPass123`
9. Password reuse → SSH as admin → user flag
10. CVE-2023-0386 (OverlayFS/FUSE) kernel exploit → root shell → root flag

**Loot:** Password hashes for root + admin, database bcrypt hashes for TRX + TheCyberGeek

**Report:** `reports/10.129.32.120/attack-report.md`

### Git
- All committed and pushed to origin/master
- Total knowledge base now: ~4700+ lines of offensive reference material
- Added 2Million attack report + scan files
